{
  "name": "opensaga-site",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i input.css -o site.css --minify",
    "watch": "tailwindcss -i input.css -o site.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
